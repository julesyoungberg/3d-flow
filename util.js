// const random = (min=-1, max=1) =>
//   Math.random() * (max - min) - (max - min) / 2;
//
// const map = (input, inMin, inMax, outMin, outMax) => {
//   const inDiff = inMax - inMin;
//   const outDiff = outMax - outMin;
//   return (input - inMin) * outDiff / inDiff + outMin;
// };
